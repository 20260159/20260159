먼저 예제 중 슈팅 파일을 선택하였다. 그리고 코드 분석을 위해 ai에게 전체 프로그램 분석을 맡겼다.
그리고 hp 시스템을 기존 시스템에서 반 칸으로 변경하도록 명령하였다. 
# 하트 표시 로직: 0.5단위 계산
    full_hearts = int(lives)
    half_heart = 1 if (lives - full_hearts) >= 0.5 else 0
    heart_str = '♥' * full_hearts + ('♡' if half_heart else '') # 반 칸은 빈 하트로 표시하거나 다른 기호 사용
    이러한 시스템을 작동하여 반 칸이 달도록 변경하였다. 반 칸의 하트 출력을 요구했지만 그림으로 구현하기 힘들었는지
    윤곽선만 남은 하트로 표현되었다. 이것도 나쁘지 않아서 추가로 수정하지는 않았다.
    그리고 pause 기능이 없는 것이 불편하여 pause 기능을 만들기로 했다.
    pause_screen() 함수:

ESC 키가 감지되면 실행됩니다.
Surface의 알파(Alpha) 값을 조절하여 현재 게임 화면 위에 어두운 반투명 막을 씌웁니다.
R 키를 누르면 RESTART를 반환하고, ESC를 다시 누르면 RESUME(재개)을 반환합니다.

이러한 형식으로 만들고 esc 시 재시작과 빠른 종료 또한 가능하게 만들었다.
그리고 충돌 로직 내부의 enemies.clear() 코드를 제거하여 내 기체가 피격 당할 시
무적이 되고 잠시동안 적들이 사라지는데 여기서 적들이 사라지는 부분만 제거하였다.
그리고 기존에 있던 레벨을 5까지 늘리고 게임 테스트를 해보며 그에 맞게 난이도를 조절하였다.
그리고 내 기체의 탄환이 적한테 잘 맞지 않는 것 같아 기존 코드에서 탄환 크기 수치를 늘렸다.
이후 확실히 잘 맞게 되었다.
그리고 적들도 탄환을 발사하면 좋을 것 같아 적들에게 탄환을 쏘는 시스템을 새로 만들었다.

enemy_shoot_timer: 300프레임(약 5초)을 세는 변수입니다. 300에 도달하면 모든 적 비행선이 탄환을 하나씩 생성합니다.
적 탄환 관리: enemy_bullets 리스트를 통해 적이 발사한 탄환들을 관리하며, 아래 방향으로 초당 약 7픽셀 속도로 이동합니다.

이런 식으로 탄환 발사까지 완성이 된줄 알았으나 탄환이 모든 기체가 동시에 발사 되어

기존의 pygame.Rect 객체 하나만 관리하던 방식에서 딕셔너리로 변경했습니다.
spawn_time: pygame.time.get_ticks()를 사용해 적이 생성된 시점의 시간을 기록합니다.
has_shot: 각 적이 탄환을 한 번만 발사하도록 체크하는 플래그입니다.

이런 식으로 바꾸어 개별적으로 탄환을 발사하도록 변경하였다.
테스트 끝에 탄환 속도를 3초마다 발사하게 변경했는데 탄환을 3초마다 발사해도 기체의
속도가 너무 빨라 탄환을 쏘는 걸 거의 볼 수 없었다. 그래서 적의 이동속도를 늦춰 탄환 발사 시스템을
적극 활용할 수 있도록 변경하였다.
그 후 새로운 적을 만들었다.

시각적 구분: 크기가 50x50으로 일반 적(36x36)보다 훨씬 크고, 색상이 진한 **보라색(PURPLE)**이며 육각형 모양입니다.
내구도: 플레이어의 탄환에 2번 맞아야 파괴됩니다.
피격 피드백: 탄환에 맞으면 소멸하지 않고, 아주 짧은 시간(5프레임) 동안 기체가 **붉은색(HURT_COLOR)**으로 깜빡거려 데미지를 입었음을 알립니다.
보상: 파괴 시 일반 적(10점)보다 높은 50점을 줍니다.
등장 방식: 레벨이 오를수록 등장 확률이 높아집니다. 레벨 1에서는 등장하지 않으며, 레벨 5에서는 생성되는 적 중 40%가 중형 적입니다.

중형 적이 작동은 잘 되는데 육각형 모양인게 어색하고 점수도 많이 줘서 모양을 기존 그대로 바꾸고 제공 점수도 줄였다.
그렇게 계속 추가하다보니 갑자기 파이 게임이 응답없음이 떴다. 너무 많이 추가해서 그런 것일까 그래서 최적화를 하기로 했다.
정확한 원인은 모르지만 ai에게 최적화를 맡겼더니 다음과 같이 되었다.

컴퓨터 사양 문제: 별의 개수(stars)가 너무 많거나 프레임 드랍이 심할 때 발생합니다. 위 코드에서는 별 개수를 80개에서 50개로 줄였습니다.
무한 루프: while True 내부에서 조건문이 꼬여 이벤트 처리를 못 할 때 발생합니다. pygame.event.get()이 매 프레임마다 확실히 호출되도록 구조를 잡았습니다.
충돌 리스트 수정: 리스트를 순회(for x in list)하면서 동시에 요소를 삭제(list.remove(x))하면 인덱스가 꼬여 시스템이 멈출 수 있습니다. 이를 방지하기 위해 복사본(list[:])을 사용하거나 컴프리헨션을 사용했습니다.

저렇게 고치다 보니 게임 오버 시스템 쪽이 싹 다 날아가서 다시 복원 시켰다.
그리고 최고 기록 시스템을 넣었다. 원래는 메인화면에 넣으려고 헀지만 게임 자체가 메인 화면 업이 바로 시작이라 
게임 오버 화면에 최고 기록 시스템을 집어 넣었다.

최고 기록 저장/불러오기: high_score.txt 파일을 생성하여 프로그램이 종료되어도 기록이 남도록 했습니다.
기록 갱신 로직: 현재 점수가 기존 최고 기록보다 높으면 실시간으로 업데이트하고 게임 오버 화면에서 강조합니다.
UI 업데이트: 게임 오버 화면 중앙에 BEST SCORE 항목을 추가했습니다.

그리고 마지막으로 적의 탄환을 내 탄환으로 부수는 탄환 충돌 시스템을 추가했다.

탄환 vs 탄환 충돌: bullets(플레이어 탄환) 리스트와 e_bullets(적 탄환) 리스트를 비교하는 반복문을 추가했습니다.
상쇄 로직: b.colliderect(eb)가 참일 경우, 플레이어 탄환과 적 탄환을 각각 리스트에서 제거합니다.
중복 처리 방지: 탄환이 적 탄환과 이미 부딪혔다면, 같은 프레임에서 적 기체와 또 부딪히는 판정이 일어나지 않도록 continue를 사용하여 다음 탄환 계산으로 넘깁니다.

이렇게 해서 일단은 마무리하였다. ai에게 명령을 구체적으로 주지 않거나 코드에 대한 이해도가 부족하다면 원하는 방향성대로 나오지 않는다는 것을 깨달았다.
